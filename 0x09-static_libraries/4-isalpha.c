#include "main.h"
/**
 * _isalpha - returns 1 if c is a letter, lowercase
 * or uppercase
 *@c: the input to test
 *
 * Return: returns the above
 */
int _isalpha(int c)
{
	return (c >= 65 && c <= 122);
}
